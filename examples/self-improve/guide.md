# Ralph Self-Improvement Guide

- Keep each iteration small and reversible.
- Prefer one targeted change per step.
- Do not use destructive git commands.
- Validate with `bash -n` and one dry-run after edits.
- If outcome is worse, revert the last tweak and try another direction.
